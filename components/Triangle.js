import styles from '../styles/Home.module.scss';

const Triangle = () => {
  return (
    <div className={styles.triangle}>
      <span className={styles.kockica}></span>
    </div>
  );
};

export default Triangle;
